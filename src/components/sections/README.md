# Sections

Full page sections (Hero, Features, Pricing, Testimonials, CTA, Footer content, etc.) will live here.
Each section is composed from primitives in `../layout`, `../typography`, `../buttons`, `../cards`, `../motion`, and `../ui-primitives` — never hardcoded.